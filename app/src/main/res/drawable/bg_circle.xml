<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- This must have a color by default so that the PorterDuff works when changing colors -->
    <solid
        android:color="@android:color/holo_green_dark"/>

    <size
        android:width="24dp"
        android:height="24dp"/>
</shape>